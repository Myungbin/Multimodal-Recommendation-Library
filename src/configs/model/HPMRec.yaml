# coding: utf-8
# @email: jinfeng.xu0605@gmail.com / jinfeng@connect.hku.hk

embedding_size: 64
feat_embed_dim: 64

n_mm_layers: 1
n_layers: [3]
knn_k: 10
mm_image_weight: 0.1
aggr_mode: ['add']
learnable_temp: [True]
ssl_temp: [0.2]
contrast_type: ['InfoNCE']
learning_rate: [0.0001]
reg_weight: [0.001]
ssl_weight: [0.0001]
algebra_dim: [4]
warm_up_epochs: 0

hyper_parameters: ["n_layers", "aggr_mode", "ssl_temp", "learnable_temp", "contrast_type", "learning_rate", "reg_weight", "ssl_weight", "algebra_dim", "warm_up_epochs"]
